# NetBuilder

Implementation start for `Downloads/netbuilder-architecture-plan-v2.md`.

Current focus: headless deterministic TypeScript simulation (`packages/sim`) with routing, pressure bands, command/replay, topology validation, and simplified load balancer mechanics.

## Commands

```bash
npm install
npm run check
```
